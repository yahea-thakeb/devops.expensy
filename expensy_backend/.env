# MongoDB connection (local, no auth)
DATABASE_URI=mongodb://localhost:27017/expensy

# Redis connection (local, no password)
REDIS_HOST=localhost
REDIS_PORT=6379

# App settings
PORT=4000
NODE_ENV=development
